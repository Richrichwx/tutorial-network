import React from 'react';
import styled from 'styled-components';

const MusicContainer = styled.div`
  width: 800px;
`;

class Login extends React.Component {
  render() {
    return (
      <MusicContainer>Login</MusicContainer>
    )
  }
}

export default Login;